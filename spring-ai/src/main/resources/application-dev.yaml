spring:
  application:
    name: spring-ai
  ai:
    openai:
      api-key: sk-525a693004904976bb3fd698ef4434f6
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
          temperature: 0.8
      embedding:
        options:
          model: text-embedding-v3
          dimensions: 1024 #向量维度
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_ai?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: cl2308764238
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10s
      # 如有密码，取消下面注释并填写
      # password: your_password
logging:
  level:
    org.springframework.ai.chat.client.advisor: debug
    com.example.ai: debug
